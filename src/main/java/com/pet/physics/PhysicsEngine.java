package com.pet.physics;

/**
 * Handles physics calculations for the pet, 
 *      such as gravity, collisions, and movement.
 * This is a placeholder for now, 
 *          but it can be expanded in the future to include more
 */
public class PhysicsEngine {

    private boolean enabled = true;

    public boolean isEnabled() {
        return enabled;
    }

    public void setEnabled(boolean enabled) {
        this.enabled = enabled;
    }
}